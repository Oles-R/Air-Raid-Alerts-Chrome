{
  "name": "ukraine-air-raid-alerts",
  "version": "1.5.1",
  "description": "Chrome extension for real-time monitoring of air raid alerts in Ukraine (api.ukrainealarm.com)",
  "author": "Oleksandr Reshetnikov <aleksandr.reshetnikov@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "sync-languages": "powershell -NoProfile -ExecutionPolicy Bypass -File sync-languages.ps1",
    "typecheck": "tsc --noEmit",
    "compile": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "package": "powershell -NoProfile -ExecutionPolicy Bypass -File build.ps1",
    "build": "npm run sync-languages && npm run typecheck && npm run compile && npm run package"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "keywords": [
    "chrome-extension",
    "ukraine",
    "air-raid-alert",
    "повітряна-тривога",
    "ukrainealarm"
  ]
}
